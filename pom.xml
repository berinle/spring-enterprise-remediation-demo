<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      ============================================================================
      OSS "LATEST" BUILD — Spring Boot 4.x with ALL VERSION PINS REMOVED
      ============================================================================
      Experiment: how far does the pure open-source path get you?

      Rules for this branch:
        1. Move to the latest Spring Boot (4.1.0).
        2. Delete every explicit <version> pin so Boot's BOM decides — the pins
           were what defeated the upgrade on the `main` branch.

      Findings are recorded in the README. The short version: everything Boot's
      BOM manages is fixed automatically, but libraries OUTSIDE the BOM
      (commons-text, commons-collections) still need manual version management,
      and the major-version jump forces real application code changes
      (Jakarta namespace + Spring Security 7 API).
      ============================================================================
    -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.broadcom.vulndemo</groupId>
    <artifactId>tanzu-vuln-demo</artifactId>
    <version>1.0.0-OSS-LATEST</version>
    <name>Tanzu Vulnerability Demo (OSS latest)</name>
    <description>Spring Boot 4.x, all version pins removed — OSS upgrade-path experiment</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- NOT managed by the Spring Boot BOM — still needs a manual version.
             This is the point: removing the pin is not an option here, because
             nothing upstream would supply one. -->
        <commons-text.version>1.15.0</commons-text.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- was pinned 3.2.2 (CVE-2022-22963) — now managed by the Spring Cloud
             2025.1.2 release train, which targets Boot 4.x → resolves 5.0.3 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-function-web</artifactId>
        </dependency>

        <!-- ============ PINS REMOVED — versions now come from Boot's BOM ============ -->

        <!-- was 2.14.1 (Log4Shell) — Boot 4.1.0 manages log4j2 2.25.4 -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
        </dependency>

        <!-- was 1.29 (CVE-2022-1471) — Boot manages snakeyaml 2.6 -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
        </dependency>

        <!-- was 1.4.199 (H2 JNDI RCE) — Boot manages 2.4.240 -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>

        <!-- ====== STILL REQUIRES A MANUAL VERSION: outside the Spring Boot BOM ====== -->

        <!-- was 1.9 (Text4Shell). Boot does NOT manage commons-text, so the pin
             cannot simply be deleted — it just has to be kept current. -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-text</artifactId>
            <version>${commons-text.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- Release train that targets Boot 4.x (spring-cloud-function 5.0.3) -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>2025.1.2</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- Boot 4.1.0 pins maven-compiler-plugin 3.15.0, which fails on this
                 toolchain ("Cannot load from object array ... this.hashes is null").
                 Pinning a known-good compiler plugin is part of the upgrade cost. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <!-- In-process javac crashes on this toolchain; fork it. -->
                    <fork>true</fork>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
