# Java / Maven
target/
*.class

# Node / Vite
frontend/node_modules/
frontend/dist/

# Built SPA bundle (regenerated by `npm run build`)
src/main/resources/static/assets/
src/main/resources/static/index.html

# Secrets — never commit the Broadcom registry token
.env
*.env

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# the rendered demo video (57MB) - regenerate with demo/record-demo.mjs + encode-mp4.swift
demo/*.mp4
